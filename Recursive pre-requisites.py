'''Put below snippet in your code and then put @bootstrap over recursive function.
Replace instances of return with yield and call this function like yield dfs()
while calling recursively otherwise simply.
Also write yield at the end of function irrespective of you are returning something or not'''

from types import GeneratorType
def bootstrap(f, stack=[]):
    def wrappedfunc(*args, **kwargs):
        if stack:
            return f(*args, **kwargs)
        else:
            to = f(*args, **kwargs)
            while True:
                if type(to) is GeneratorType:
                    stack.append(to)
                    to = next(to)
                else:
                    stack.pop()
                    if not stack:
                        break
                    to = stack[-1].send(to)
            return to
    return wrappedfunc
